NG_APP_API_BASE_URL=http://localhost:2077/api

# Llave publicable de Stripe en modo test (dashboard.stripe.com/test/apikeys) —
# es segura de exponer en el frontend, a diferencia de la secret key del backend.
NG_APP_STRIPE_PUBLISHABLE_KEY=
