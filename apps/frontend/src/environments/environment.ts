// Configuración usada en desarrollo (`ng serve`, sin --configuration production).
// El build de producción reemplaza este archivo por environment.prod.ts
// (ver "fileReplacements" en angular.json).
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:2077/api',
};
